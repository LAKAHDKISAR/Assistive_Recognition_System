class AppConfig{
    const AppConfig._(); // private constructor to prevent instantiation

    static const String appName = 'Assistive Recognition System'; //static cause no need for instant and const such that name is known at compile time and cannot be changed.
}